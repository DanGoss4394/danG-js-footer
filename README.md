# DanG-JS-Footer

> This should be used in the following manner

Install with the command:

```
npm install --save danG-js-footer
```

Add to a JS project with the following code:

```JS
import {footer} from "danG-js-footer"

footer('Some Name')
```
